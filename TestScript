-- Créer un ScreenGui pour le menu admin
local player = game.Players.LocalPlayer
local playerName = player.Name

-- Vérifier si le joueur est un administrateur (selon son rôle ou son nom)
local adminList = {"AdminName1", "AdminName2", "AdminName3"}  -- Remplace avec les noms des admins
local isAdmin = false

for _, admin in pairs(adminList) do
    if playerName == admin then
        isAdmin = true
        break
    end
end

if not isAdmin then
    return  -- Si le joueur n'est pas un admin, ne rien afficher
end

-- Créer la GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = player.PlayerGui

-- Créer la frame du menu
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 300, 0, 400)
frame.Position = UDim2.new(0.5, -150, 0.5, -200)
frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
frame.Parent = screenGui

-- Créer un titre pour le menu
local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 0, 50)
titleLabel.Text = "Admin Menu"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextSize = 22
titleLabel.TextAlignment = Enum.TextAlignment.Center
titleLabel.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
titleLabel.Parent = frame

-- Créer un bouton pour donner des pouvoirs (par exemple, Fly)
local flyButton = Instance.new("TextButton")
flyButton.Size = UDim2.new(0, 280, 0, 50)
flyButton.Position = UDim2.new(0.5, -140, 0, 80)
flyButton.Text = "Activate Fly"
flyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
flyButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
flyButton.Parent = frame

flyButton.MouseButton1Click:Connect(function()
    -- Activer un effet de vol pour le joueur
    print(playerName .. " activated Fly!")
    -- Code pour activer le vol ici
end)

-- Créer un bouton pour donner des pouvoirs de Speed
local speedButton = Instance.new("TextButton")
speedButton.Size = UDim2.new(0, 280, 0, 50)
speedButton.Position = UDim2.new(0.5, -140, 0, 150)
speedButton.Text = "Activate Speed"
speedButton.TextColor3 = Color3.fromRGB(255, 255, 255)
speedButton.BackgroundColor3 = Color3.fromRGB(0, 0, 255)
speedButton.Parent = frame

speedButton.MouseButton1Click:Connect(function()
    -- Activer un effet de vitesse pour le joueur
    print(playerName .. " activated Speed!")
    -- Code pour augmenter la vitesse ici
end)

-- Créer un bouton pour rendre le joueur invincible (GodMode)
local godModeButton = Instance.new("TextButton")
godModeButton.Size = UDim2.new(0, 280, 0, 50)
godModeButton.Position = UDim2.new(0.5, -140, 0, 220)
godModeButton.Text = "Activate GodMode"
godModeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
godModeButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
godModeButton.Parent = frame

godModeButton.MouseButton1Click:Connect(function()
    -- Activer GodMode pour rendre le joueur invincible
    print(playerName .. " activated GodMode!")
    -- Code pour rendre invincible ici
end)

-- Créer un bouton pour fermer le menu
local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 280, 0, 50)
closeButton.Position = UDim2.new(0.5, -140, 0, 290)
closeButton.Text = "Close Menu"
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.BackgroundColor3 = Color3.fromRGB(255, 165, 0)
closeButton.Parent = frame

closeButton.MouseButton1Click:Connect(function()
    -- Fermer le menu admin
    screenGui:Destroy()
end)
